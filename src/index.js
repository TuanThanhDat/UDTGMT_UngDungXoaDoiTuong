import React from 'react';
import ReactDOM from 'react-dom';
import CanvasApp from './CanvasApp'; // Sửa đổi đường dẫn và tên tệp

ReactDOM.render(
  <React.StrictMode>
    <CanvasApp />
  </React.StrictMode>,
  document.getElementById('root')
);
